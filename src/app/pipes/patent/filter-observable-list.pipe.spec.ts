import { FilterObservableListPipe } from './filter-observable-list.pipe';

describe('FilterObservableListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObservableListPipe();
    expect(pipe).toBeTruthy();
  });
});
