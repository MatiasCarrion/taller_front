import { ExistePatentePipe } from './existe-patente.pipe';

describe('ExistePatentePipe', () => {
  it('create an instance', () => {
    const pipe = new ExistePatentePipe();
    expect(pipe).toBeTruthy();
  });
});
